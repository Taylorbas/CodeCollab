//
//  UpdateListener.swift
//  CodeCollab
//
//  Created by Bassil Taylor on 11/5/24.
//
/*
import Firebase

func listenForUpdates() {
    let db = Firestore.firestore()
    db.collection("transactions").addSnapshotListener { snapshot, error in
        guard let changes = snapshot?.documentChanges else { return }
        for change in changes {
            if change.type == .added {
                // Trigger notification
                let notification = NotificationBanner(message: "New transaction confirmed!")
                // Display notification in the UI
            }
        }
    }
}
*/
